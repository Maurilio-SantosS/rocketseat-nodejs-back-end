// Representação de um espaço da mémoria do computador usada para transmitir dados rapidamente (formato binário hexadecimal)
const buf = Buffer.from("hello")

// Dado em hexadecimal
console.log(buf)

// Dado em decimal
console.log(buf.toJSON())